export type LogUser = {
    username: string,
    password: string
}
export type RegisterUser = {
    username: string,
    name: string,
    password: string
}
export type User = {
    id: number,
    username: string,
    name: string
}