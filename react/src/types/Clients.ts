export type Client = {
    id: string
    dni: string
    name: string
    email: string
    phone: string
  }

export type RegisterClient = {
  dni: string
  name: string
  email: string
  phone: string
}